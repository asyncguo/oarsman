import Foundation

extension Notification.Name {
    /// Posted when a todo should be surfaced in the main window.
    static let todoSelectionRequested = Notification.Name("TodoSelectionRequested")
}
